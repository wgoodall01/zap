// This file is auto-generated by @hey-api/openapi-ts

import type { Options as ClientOptions, TDataShape, Client } from './client';
import type { ActivityLeaderboardData, ActivityLeaderboardResponses, ActivityLeaderboardErrors, AuthMeData, AuthMeResponses, AuthMeErrors, HealthcheckData, HealthcheckResponses, HealthcheckErrors, GetCheckinsData, GetCheckinsResponses, GetCheckinsErrors, AuthGetUserData, AuthGetUserResponses, AuthGetUserErrors, WarmupData, WarmupResponses, WarmupErrors, WolpStreamData, WolpStreamErrors, ZapDeviceListData, ZapDeviceListResponses, ZapDeviceListErrors, ZapTriggerData, ZapTriggerResponses, ZapTriggerErrors } from './types.gen';
import { client as _heyApiClient } from './client.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = ClientOptions<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Get the activity leaderboard for the most active users
 */
export const activityLeaderboard = <ThrowOnError extends boolean = false>(options?: Options<ActivityLeaderboardData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ActivityLeaderboardResponses, ActivityLeaderboardErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/activity/leaderboard',
        ...options
    });
};

/**
 * Get the current authenticated user's invoker information
 */
export const authMe = <ThrowOnError extends boolean = false>(options?: Options<AuthMeData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<AuthMeResponses, AuthMeErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/auth/me',
        ...options
    });
};

/**
 * Healthcheck endpoint
 */
export const healthcheck = <ThrowOnError extends boolean = false>(options?: Options<HealthcheckData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<HealthcheckResponses, HealthcheckErrors, ThrowOnError>({
        url: '/healthcheck',
        ...options
    });
};

/**
 * Fetch LA Fitness check-in history
 * This endpoint logs into the LA Fitness website, scrapes the check-in history,
 * and returns a list of check-ins sorted most-recent-first.
 *
 * This is an unauthenticated endpoint - the LA Fitness credentials are provided
 * in the request body.
 */
export const getCheckins = <ThrowOnError extends boolean = false>(options: Options<GetCheckinsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<GetCheckinsResponses, GetCheckinsErrors, ThrowOnError>({
        url: '/lafitness/get_checkins',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};

/**
 * Get a user by their UUID
 */
export const authGetUser = <ThrowOnError extends boolean = false>(options: Options<AuthGetUserData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<AuthGetUserResponses, AuthGetUserErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/user/{id}',
        ...options
    });
};

/**
 * Warmup route.
 * On Lambda, we need to warm up a couple things:
 * - The Aurora database, which will take ~10s to wake up from cold start.
 * - The Lambda, which is already warm by the time this code runs, but is still triggered
 * by the incoming HTTP request.
 *
 * The frontend will call this route when it loads, so that we start warming up all the stuff (hopefully) before the user tries to do anything.
 *
 * This is an unauthenticated route, because we want to be able to call it before the user
 * needs to use the API to sign in.
 */
export const warmup = <ThrowOnError extends boolean = false>(options?: Options<WarmupData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<WarmupResponses, WarmupErrors, ThrowOnError>({
        url: '/warmup',
        ...options
    });
};

/**
 * Redirect to a stream M3U8 URL from hdontap service
 */
export const wolpStream = <ThrowOnError extends boolean = false>(options: Options<WolpStreamData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<unknown, WolpStreamErrors, ThrowOnError>({
        url: '/wolp/stream',
        ...options
    });
};

/**
 * List all OpenShock devices and shockers for the authenticated user
 */
export const zapDeviceList = <ThrowOnError extends boolean = false>(options?: Options<ZapDeviceListData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ZapDeviceListResponses, ZapDeviceListErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/zap/device',
        ...options
    });
};

/**
 * Trigger an action on one or more OpenShock devices
 */
export const zapTrigger = <ThrowOnError extends boolean = false>(options: Options<ZapTriggerData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ZapTriggerResponses, ZapTriggerErrors, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/zap/trigger',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options.headers
        }
    });
};